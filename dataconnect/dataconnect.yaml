specVersion: "v1alpha"
serviceId: "complainet-api"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "complainet"
      cloudSql:
        instanceId: "complainet-instance"
connectorDirs: ["./connector"]
